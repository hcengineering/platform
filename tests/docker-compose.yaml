services:
  network:
    image: hardcoreeng/network-pod
    pull_policy: never
    extra_hosts:
      - 'huly.local:host-gateway'
    environment:
      - NETWORK_POD_PORT=3737
      - NETWORK_POD_HOST=*
    ports:
      - 37371:3737
    restart: unless-stopped
