services:
  account:
    environment:
      - DB_URL=${DB_PG_URL}
  transactor:
    environment:
      - DB_URL=${DB_PG_URL}
  workspace:
    environment:
      - DB_URL=${DB_PG_URL}
  fulltext:
    environment:
      - DB_URL=${DB_PG_URL}
