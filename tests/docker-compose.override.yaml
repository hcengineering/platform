services:
  account:
    environment:
      - DB_URL=${DB_PG_URL}
      - PROCEED_V7_MONGO=false
  transactor:
    environment:
      - DB_URL=${DB_PG_URL}
  workspace:
    environment:
      - DB_URL=${DB_PG_URL}
  fulltext:
    environment:
      - DB_URL=${DB_PG_URL}
